Valentine Website

Open `index.html` in your browser to view the interactive valentine envelope. Click or press Enter/Space on the envelope to open it and reveal the message.

Files:
- index.html — page
- styles.css — styling and animation
- script.js — click/keyboard interaction

To run locally: open `index.html` (no server required).